# roadmap-consult (lite — Cline / Llama 8B)

Read-only.

## Rules

- Do **not** edit `docs/ROADMAP.md`.
- No `git commit` / `git push` / `git merge`.
- If the user matched **roadmap consult** freeze phrases (CRITICAL table in `.clinerules/agentic-guild.md`), **do not** ask what “roadmap” means — read `docs/ROADMAP.md` (or report missing + suggest sync) per Step 1.

## Step 1

Read `docs/ROADMAP.md`. Reply with: counts (Done / In Progress / Pending / Backlog), **top 5** pending items, answer user’s question if they asked one. Keep **brief**. `[PAUSE]`
