# pr-description (lite — Cline / Llama 8B)

## Rules

- **One step per reply.**
- No `git commit` / `git push` / `git merge`.
- **No markdown links** to repo paths — use backticks only (e.g. `docs/core/SPEC.md`), never `[text](path)`.

## Preconditions

`docs/core/SYSTEM_ARCHITECTURE.md` and `docs/core/SPEC.md` should exist.

## Stealth

If `.agenticguild/config.json` has `"stealth_mode": true`, do not mention stealth.

## Step 1

Summarize branch intent from diff + recent commits. Output PR body in a **single markdown code block** for copy-paste: Summary / Changes / Testing / Notes. Keep **under ~40 lines**. `[PAUSE]`
