# hello (lite — Cline / Llama 8B)

## Rules

- **One step per reply.** Stop after step 1 unless user continues.
- No `git commit` / `git push` / `git merge` (suggest messages only).
- If the user matched **hello** freeze phrases (see `.clinerules/agentic-guild.md` CRITICAL table), **do not** ask whether they mean you vs the app vs the repo — run this step immediately.

## Step 1 — Intro + health check

1. Greet briefly as **agentic:guild** (senior engineer, process-driven).
2. Check if these exist (read if needed): `docs/core/SYSTEM_ARCHITECTURE.md`, `docs/core/SPEC.md`, `docs/core/deterministic_coding_standards.md`, `docs/ROADMAP.md`.
3. Report **missing** files in one short list; offer to help create them.
4. Give **3 copy-paste phrases**: “explore task …”, “start task …”, “finish branch / open PR”.
5. Ask what they want to do next.

**Then stop** `[PAUSE]`.
